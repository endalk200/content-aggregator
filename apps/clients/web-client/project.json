{
    "$schema": "../../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "apps/clients/web-client",
    "projectType": "application",
    "targets": {
        "build": {
            "executor": "@nrwl/next:build",
            "outputs": ["{options.outputPath}"],
            "defaultConfiguration": "production",
            "options": {
                "root": "apps/clients/web-client",
                "outputPath": "dist/apps/clients/web-client"
            },
            "configurations": {
                "development": {
                    "outputPath": "apps/clients/web-client"
                },
                "production": {}
            }
        },
        "serve": {
            "executor": "@nrwl/next:server",
            "defaultConfiguration": "development",
            "options": {
                "buildTarget": "web-client:build",
                "dev": true
            },
            "configurations": {
                "development": {
                    "buildTarget": "web-client:build:development",
                    "dev": true
                },
                "production": {
                    "buildTarget": "web-client:build:production",
                    "dev": false
                }
            }
        },
        "export": {
            "executor": "@nrwl/next:export",
            "options": {
                "buildTarget": "web-client:build:production"
            }
        },
        "test": {
            "executor": "@nrwl/jest:jest",
            "outputs": ["coverage/apps/clients/web-client"],
            "options": {
                "jestConfig": "apps/clients/web-client/jest.config.ts",
                "passWithNoTests": true
            }
        },
        "lint": {
            "executor": "@nrwl/linter:eslint",
            "outputs": ["{options.outputFile}"],
            "options": {
                "lintFilePatterns": ["apps/clients/web-client/**/*.{ts,tsx,js,jsx}"]
            }
        }
    },
    "tags": []
}
